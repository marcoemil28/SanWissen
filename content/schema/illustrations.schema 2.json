{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "illustrations.schema.json",
  "title": "Abbildungen",
  "description": "Bildunterschrift je Abbildung. Die Datei liegt als content/images/illu-<id>.jpg daneben.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Verweis auf dieses Schema. Nur für den Editor, die Apps ignorieren das Feld."
    },
    "illustrations": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string"
          },
          "caption": {
            "type": "string",
            "description": "Darf leer sein, dann zeigen die Apps keine Unterschrift."
          }
        },
        "required": [
          "id",
          "caption"
        ]
      }
    }
  },
  "required": [
    "illustrations"
  ]
}
